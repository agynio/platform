export { default as NodeDetailsPanel } from './NodeDetailsPanel';

// Note: StaticConfigForm and DynamicConfigForm were removed with the
// migration to custom ConfigViews; forms are now rendered via RightPropertiesPanel registry.
