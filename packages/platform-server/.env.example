# Copy this file to .env and adjust values as needed
# Postgres for persistent conversation state (Prisma) and graph checkpoints.
# Dedicated agents-db is exposed on 5443. Example:
#   postgresql://agents:agents@localhost:5443/agents
# You can also point to any Postgres using a standard URL.
# AGENTS_DB_* variables (used by docker-compose agents-db):
#   AGENTS_DB_USER (default: agents)
#   AGENTS_DB_PASSWORD (default: agents)
#   AGENTS_DB_NAME (default: agents)
# Node id (nodeId) is required for deterministic upsert and is provided by the Agent node.
AGENTS_DATABASE_URL=

# LiteLLM admin setup (replace master key with your actual secret)
LITELLM_BASE_URL=http://127.0.0.1:4000
LITELLM_MASTER_KEY=sk-dev-master-1234
# Optional LiteLLM tuning (virtual key alias agyn_key is automatic)
# LITELLM_MODELS=gpt-4o
# LITELLM_KEY_DURATION=30d
# LITELLM_MAX_BUDGET=100
# LITELLM_RPM_LIMIT=600
# LITELLM_TPM_LIMIT=90000
# LITELLM_TEAM_ID=

# Optional: GitHub integration (App or PAT). Safe to omit for local dev.
# GITHUB_APP_ID=
# GITHUB_APP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GITHUB_INSTALLATION_ID=
# GH_TOKEN=
# GITHUB_WEBHOOK_SECRET=
# GITHUB_API_URL=
# GITHUB_BASE_URL=

# Optional: HashiCorp Vault (dev)
# Enable to use Vault-backed env vars and autocomplete endpoints
VAULT_ENABLED=false
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=dev-root

# (Slack env removed intentionally; no global Slack config or tokens)

# Workspace container network name (must exist locally)
WORKSPACE_NETWORK_NAME=agents_net

# Nix cache proxy (ncps) endpoints
# NCPS_URL_SERVER=http://localhost:8501
# NCPS_URL_CONTAINER=http://ncps:8501

# Workspace volume GC tuning (defaults shown)
# VOLUME_GC_ENABLED=true
# VOLUME_GC_INTERVAL_MS=60000
# VOLUME_GC_MAX_PER_SWEEP=100
# VOLUME_GC_CONCURRENCY=3
# VOLUME_GC_COOLDOWN_MS=600000

# Container retention window (in days). Set to 0 to retain indefinitely.
CONTAINERS_RETENTION_DAYS=14
